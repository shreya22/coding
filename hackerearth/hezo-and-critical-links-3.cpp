#include <bits/stdc++.h>
using namespace std;

#define FOR(i, n) for(int i=0; i<n; ++i)
#define FOR1(i, n) for(int i=1; i<n; ++i)
#define FORrev(i, n) for(int i=n-1; i>=0; --i)
#define pb push_back
#define ll long long

#define MAX 100001
int n, m, p;
int num_ways[MAX][MAX];
vector<int>

int main() {

    cin>>n>>m>>p;

    int a, b;
    FOR(i, n)
    {

    }

    return 0;
}
